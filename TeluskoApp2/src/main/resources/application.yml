spring:
  application:
    name: TeluskoApp2

#    eureka

  boot:
    admin:
      client:
        url: http://localhost:4141/

server:
  port: 8485

management:
  endpoints:
    web:
      exposure:
        include: '*'
        
